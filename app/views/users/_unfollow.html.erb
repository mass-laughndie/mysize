<%= form_tag(current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete }) do |f| %>
  <% button_tag fa_icon("user") + fa_icon("check"), class: "follow-button" %>
<% end %>