<div class="form-unfollow">
  <%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
               html: { method: :delete },
               remote: true) do |f| %>
    <% f.button fa_icon("user") + fa_icon("check") %>
  <% end %>
</div>