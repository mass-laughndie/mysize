<%= form_tag(current_user.active_relationships.build) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= button_tag fa_icon("user") + fa_icon("plus"), class: "follow-button accent" %>
<% end %>