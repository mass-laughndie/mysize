<% provide(:title, "通知") %>

<% if logged_in? && @notices.any? %>
<div class="content-nspace">
  <ul>
    <%= render @notices %>
  </ul>
</div>
<% else %>
  <%= render partial: 'shared/no_data',
                 locals: { target: "notice" } %>
<% end %>